# Raise SpEL expression length limit from default 10,000 to 500,000.
# Required to support base64 image data URIs in Thymeleaf templates.
# This is read by org.springframework.core.SpringProperties BEFORE system properties.
spring.context.expression.maxLength=500000
