# ════════════════════════════════════════════════════════════
#  Acquira Platform — .gitignore
# ════════════════════════════════════════════════════════════

# ─── Maven Build Output ───
target/
!.mvn/wrapper/maven-wrapper.jar
!**/src/main/**/target/
!**/src/test/**/target/
*.class
*.jar
!.mvn/wrapper/*.jar

# ─── IDE ───
.idea/
*.iws
*.iml
*.ipr
.vscode/
.classpath
.project
.settings/
*.swp
*.swo
*~

# ─── Node.js / React Frontend ───
frontend/node_modules/
frontend/dist/
frontend/build/
frontend/.env.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ─── Runtime Data (generated at runtime — never commit) ───
logs/
reports/
data/
temp/
backups/

# ─── Playwright Browser Binaries ───
.cache/ms-playwright/

# ─── Spring Boot ───
*.dump
hs_err_pid*

# ─── Log Files ───
*.log
compile*.log
build.log
temp_log.txt

# ─── OS Files ───
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# ─── Large Data Files (samples, uploads, test data) ───
*.xlsx
*.csv
*.sql.gz
test_report.pdf

# ─── Environment / Secrets ───
.env
.env.*
!.env.example
application-prod.properties

# ─── PowerShell Utility Scripts (local dev only) ───
*.ps1

# ─── Migration Artifacts ───
# Root-level scratch folder for one-off migration exports/dumps.
migration/
# ...but the Flyway-style scripts under db/migration ARE source: application.properties
# lists them in spring.sql.init.schema-locations, so a clone without them cannot boot.
!acquira-core/src/main/resources/db/migration/
# ...except the local scratch copies sitting inside it.
acquira-core/src/main/resources/db/migration/New folder/
acquira-core/src/main/resources/db/migration/New folder.zip
MIGRATION_GUIDE.md

# ─── Root-level loose src folder (if any — NOT the module src folders) ───
/src/
